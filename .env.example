# Local development only. Use production with HTTPS on the server.
APP_ENV=development
# Optional absolute path OUTSIDE the web document root.
# DB_PATH=/absolute/private/path/app.sqlite
# Supply ADMIN_PHONE, ADMIN_NAME and ADMIN_PASSWORD temporarily through your shell.
# Never put real administrator credentials in this example or commit a .env file.
